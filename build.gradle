plugins {
    id 'java'
    id 'maven-publish'
}

group project.group
version project.version

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifact(jar) {
                builtBy jar
            }
        }
    }

    repositories {
         mavenLocal()
    }
}
